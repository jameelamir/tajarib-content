FROM node:20-alpine

RUN apk add --no-cache docker-cli docker-cli-compose git bash

WORKDIR /repo

COPY scripts/webhook-receiver.js /webhook-receiver.js

EXPOSE 9000

CMD ["node", "/webhook-receiver.js"]
